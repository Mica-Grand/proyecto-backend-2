<h1>{{title}}</h1>

<div>
    <ul>
        {{#each products}}
            <li>
                <h2>{{title}}</h2> 
                <img src={{{thumbnails}}}>
                <p>{{description}} </p> 
                <p>{{price}}</p> 
            </li>
        {{/each}}
    </ul>
</div>
